/*
 * Service worker — caches the whole app so it runs OFFLINE once installed
 * (works with your PC off, even if the host is down).
 *
 * IMPORTANT: bump CACHE (e.g. elg-v2) whenever you change any cached file,
 * otherwise installed phones keep serving the old version from cache.
 *
 * Note: service workers only run over HTTPS (or localhost) — not over a plain
 * http:// LAN address. So offline install works on the hosted (https) site.
 */
var CACHE = "elg-v82";  /* The Grid's generator learns what v81 taught the game. First run of the THIN-CELL AUDIT (answer counts for all 1,659 combos the generator can deal) caught a defect v81 itself introduced: once clubs.js declared Tau Ceramica and Baskonia the same club, a board could still deal them as two SEPARATE criteria — and two of the eighty live grids did, the same club twice, two near-identical columns competing for the same players under the each-player-once rule. Alias-source names now stay valid as ANSWERS but can no longer become criteria (canonicalOnly filter in build_grids.js); grids regenerated, none carry an era name as a header. The audit's other numbers, recorded for batch 2: 587 viable combos, 178 of them at the bare 2-answer minimum; the thinnest entities are FC Barcelona, ASVEL, Baskonia, Fortitudo Bologna and Anadolu Efes among clubs, France, Slovenia and Serbia among nationalities; and 120 dead club×nationality combos sit exactly ONE player from becoming dealable — the highest-leverage additions there are. Previously, v81 — The Grid learns the MIDDLE GENERATION, and Baskonia gets its 2000s back. A Reddit reviewer called the Grid thin — he'd drawn Olympiacos×Lithuania and Baskonia×Lithuania and watched Macijauskas, Kuzminskas and Rokas Giedraitis all get rejected, none of them one-season players — and he was right twice over. FIRST, the missing players: the database held current rosters plus canonised legends and almost nobody in between, so the well-travelled 2005–2024 veterans a fan actually reaches for didn't exist. THIRTY of them do now, researched and cross-verified (two sources minimum, five parallel researchers): the three he named plus Nocioni, Ante Tomic, Jaycee Carroll, San Emeterio, Claver, Ribas, Maciulis, Kalnietis, Pocius, Pero Antic, Marjanovic, Macvan, Stimac, Zoran Dragic, Perperoglou, Papapetrou, Lojeski, Pleiss, Djedovic, Shved, Tyrese Rice, Sonny Weems, Devin Smith, Charles Jenkins, Goudelock, Udoh and Nunnally. Legends 173→203, careers 466→496; ten of the thirty already started Final Four fives in lineups.js, so names the site itself displays finally resolve as guesses. SECOND, the era-name bug: careers file Baskonia's 2000s era-accurately as "Tau Ceramica", and the Grid matched clubs by exact string — so Scola, Splitter, Garbajosa, Oberto and Rakocevic could NEVER answer a Baskonia cell. clubs.js (the one shared map of when two names mean the same club) now folds the Tau Ceramica / Caja Laboral / Laboral Kutxa lineage into Baskonia and Caja San Fernando into CB Sevilla, and thegrid.js registers every stint under its era name AND its canonical one — with build_grids.js loading the same map, because the generator and the game sharing one definition of "played for X" is the whole point of the generator loading the real game. Along the way TEAM_FIX gained a dozen sponsor-era foldings (ALBA/Alba Berlin, Rytas Vilnius→Lietuvos Rytas, Iberostar and La Laguna Tenerife→CB Canarias, Shanxi Loongs→Brave Dragons…) including one PRE-EXISTING split it healed: Spartak St. Petersburg and Spartak Saint Petersburg were both live. Paths, grids, puzzles and odd-one-outs regenerated from the wider universe — dailies re-deal, records and streaks untouched. Worth recording: careers keep era-accurate names by design, so a short stint that shares a from-year with a longer neighbour gets absorbed by clean() as a cameo — Kuzminskas's half-season at Olympiacos survived only as 2019–2020, which is also what the season convention says it should be. Tests 638. Previously, v80 — The feedback form can finally SEND. v75 built everything around the send — the form, the relay plumbing, the honeypot, the draft stash, the mailto fallback — and shipped with FB_KEY empty, so for three weeks every press of Send answered "Sending isn't switched on yet" and offered the mail route instead. The form was not broken; it was never switched on, exactly as its own copy admitted. The Web3Forms access key now exists (created 2026-08-27 against the site's inbox) and is pasted in. It was verified BEFORE shipping with a browser-origin POST from euroballgames.com itself — worth recording: api.web3forms.com sits behind a Cloudflare bot challenge, so a curl-style test returns a challenge page no matter how valid the key is, and only proves the tester is not a browser. The key is PUBLIC by design: it names the destination inbox without revealing the address, so the scraper guard — address split in two and assembled at runtime, never present in the page source, enforced by a test — survives intact. Nothing else changes: a ticked honeypot is still swallowed and told it succeeded, a 200 that says success:false is still a refusal, and a failed send still stashes the draft to localStorage and reveals the mail route carrying every typed word. Previously, v79 — Every game now has a PAGE, and the site can be measured. Two changes, one purpose: until today euroballgames.com was a single document. Eleven games lived at ?game=mystery … ?game=rostermaster and every one of those URLs served byte-identical HTML — the game you saw was decided by JavaScript after the page loaded. v70 stopped those URLs competing with each other by self-canonicalising them, which was right and did nothing for the underlying problem: a crawler was still being shown one thin page eleven times, while Sportdle's EuroLeague game sits on its own path under about 1,200 words of copy and takes the search traffic. NEW FILE build_pages.js writes a real directory per game — /mystery-player/, /the-grid/, /path-between/ — each a complete, immediately playable copy of the app whose title, description, canonical, OG tags, h1, how-to prose and FAQ belong to that game and no other, with VideoGame + FAQPage + BreadcrumbList JSON-LD in the head. They are GENERATED: the shell always comes from index.html, so an app change reaches all eleven on the next `node build_pages.js`, and test.js fails if the built pages have drifted from it. The copy lives in build_pages.js rather than a generated data file because it is written, not computed — build_grids.js could rebuild its output tomorrow; nobody can regenerate a sentence. The slug names the GAME, not the view id, which is why clubreveal lands at /common-club/: renamed in v76, and the address a person reads should say what the game is called now, while the localStorage key must not move or real streaks break. Three things had to change with it. (1) The HUB TILES ARE LINKS. They were <button>s, and a button is not a route — nothing that declines to run scripts can follow one, so all eleven new pages were orphaned the moment they were written, reachable only via the sitemap. They are <a href> now; app.js intercepts the ordinary click exactly as before and deliberately does NOT touch a ctrl/cmd/shift/middle click, so open-in-new-tab works for the first time. build_pages.js rewrites those hrefs to ../ in the generated copies, because the lobby travels inside every page and href="the-grid/" read from /path-between/ would resolve to /path-between/the-grid/. (2) ROUTING IS PATH-AWARE. urlFor writes /the-grid/ instead of ?game=thegrid, off a siteRoot() resolved from a per-document window.__ELG_ROOT__ ("./" on the hub, "../" one level down) rather than a hardcoded "/" — that single indirection is what keeps the github.io mirror, served from /eurogame/, working off the same build. Old ?game= links are kept working FOREVER and rewritten to the new path on arrival, folding the old address into the new one instead of leaving two live URLs per game. Records and Archive keep the query form on purpose: they show nothing but your own saved scores, so they get no page, and a path with no file behind it would 404 on refresh. (3) SERVE.JS SERVES DIRECTORY INDEXES, or all eleven pages would 404 locally while working perfectly in production. Also: ANONYMOUS PAGE COUNTS, via Cloudflare Web Analytics — no cookies, no localStorage, no fingerprint, nothing that survives the request, host-gated so a local session and the github.io mirror stay out of the numbers, and it does not ship until a token is pasted in. The footer's promise was reworded from "no tracking" to "no cookies", because the honest claim is the specific one. That beacon also exposed a real BUG in this file: the fetch handler assumed every request was same-origin, which held only because the sole outside call was the feedback POST, and POSTs return one line earlier. A cross-origin GET is not a navigation and does not end in .html/.js/.css, so the beacon would have fallen to the CACHE-FIRST branch, been stored under the app's cache key, and — on the failed request that an offline-first PWA sees routinely — been answered with index.html. A page of HTML, handed to a script tag, cached until the next bump. Cross-origin requests now return untouched. Previously, v78 — Every game with a text field now suggests as you type, except Roster Master. In practice that meant ONE gap: Mystery Player, Player ID, Complete the Five, The Grid and (as of v77) Path Between already had lists, and Connections / Career Order / Odd One Out / Higher or Lower have no text field at all — so this is Common Club catching up. It needed more thought than the others, because unlike Path Between’s 466 player names, Common Club’s pool IS the answer space: 22 clubs, which is exactly why v69 pulled it. What settles it is that the list never says which of the matches the two players actually shared, and any EuroLeague follower can already recite the twenty-two — so what it removes is the spelling of "Crvena Zvezda" and "Anadolu Efes", not the deduction. Two safeguards keep that honest: NOTHING appears until you type, so the 22 can never be browsed as a menu of the answers, and clubs you have already tried drop out of the list (the resolver still knows them, so retyping one is reported for free rather than costing a guess). Arrows and Enter to pick; Escape closes the list and keeps what you typed, a second Escape clears the field. Roster Master is deliberately left out: it asks you to name a whole roster, so a list would let you walk it instead of recalling it. Previously, v77 — Path Between gets its suggestion list back. Names now pop up as you type — arrows and Enter to pick, or type straight through. What it lists is the point: EVERY player in careers.js whose NAME matches, minus the ones already in your chain, and it never marks which of them actually links from your current end. So it spells Spanoulis for you without answering the question, and most of what it offers is a dead end. v69 removed this alongside Common Club’s club list; that was right there (a menu of twenty-two clubs IS the answer set) and an overcorrection here, where the list was only ever a name index over 466 players. Typed resolution stays exactly as it was and is still the path Enter takes with no suggestion highlighted — a pasted name, a full name, or the list dismissed with Escape, which now closes the list but KEEPS what you typed rather than wiping the field. The list closes on a settled round, on a new deal, and on a committed guess; already-used names get their own wording ("already in your chain") instead of pretending the player doesn’t exist. Common Club deliberately does NOT get one. Worth recording for the next person who screenshots this app: headless Chrome under --virtual-time-budget does not advance CSS transitions, so every animated element renders in its START state — mode tabs looked like the wrong one was selected, banners looked half-faded, and hub tiles looked grey. The DOM was right the whole time. Disable transitions before trusting a screenshot. Previously, v76 — Club Reveal is now COMMON CLUB, and a different puzzle. It used to reveal one club’s roster name by name, obscure first, and score you on how few names you needed. Now you get TWO players whose careers cross at exactly one club, and that club is the answer — 3 guesses, scored on how few you use. Nothing is revealed any more, so “Reveal next” is gone and the name went with it. THE GUARANTEE is one right answer: a pair only becomes a puzzle if it shares exactly one club across all 465 clubs in careers.js, not merely one of the ~22 askable ones — checking only the askable ones would have been the easy mistake, since two players who also crossed at some second-division club still have two crossings and a player who names the obscure one is right. Eras do NOT have to overlap: both merely wore the shirt, possibly decades apart (overlapping years remain Path Between’s game, which is what teammates means there). The Daily is anchored on a Final Four starter so there is always a name to hold on to; Active/Legends/Both keep their tabs, and Both now reaches widest of all because a legend beside a current player only became legal once eras stopped having to match. No data file: pairs come off careers.js at deal time, indexed by club so one club’s members get paired up (~2ms) instead of all 108,000 combinations (~90ms, a visible hitch on a phone). NEW FILE clubs.js holds the club-name alias map that Path Between used to keep privately — measured, folding the three Levallois-era spellings together rejects exactly 2 pairs (Poirier+Prepelic, Prepelic+Sako) that would otherwise have shipped claiming a single shared club while quietly sharing a second; the other thirteen entries are defensive until a roster update introduces their second spelling. Two copies of that map drifting apart would have broken Common Club silently while Path Between kept working, which is the worst way to find out. Records and Archive keep the elg:cv: keys and the ?game=clubreveal URL, so old links and the hub streak still work; a daily saved by the old roster game can’t restore, because the save now has to name the same two players. Previously, v75 — Feedback is a form you can type into. Both earlier versions asked the reporter to compose the mail themselves, and the last hop was always a mailto: it resolves to NOTHING on a machine with no default mail app (the original silent dead end), and even after the modal put the address on screen, saying anything still meant leaving the site. Now the modal takes a NAME and a MESSAGE and posts them to a form relay, so Send sends from any machine — this site has no backend of its own, so a typed message needs somewhere to go. The relay key is public by design and reveals only the destination inbox, so the scraper guard holds: the address is still absent from the page source and written in at open time. The name is optional and the message is not — rejecting a bug report over a missing name loses the report. The mail route survives as the FALLBACK, revealed only once a send has actually failed (offline, relay down, no key), carrying what was typed; unsent words are stashed to localStorage, so a closed tab or a stray Escape costs nothing, and the name is remembered for the next report. A ticked honeypot is dropped and told it succeeded. A 200 that says success:false is a refusal, not a send. Previously, v74 — BUGFIX: The Grid's reveal could strand a cell and print "—", making a solvable puzzle look broken. It took the first free answer per cell, in order, but neighbouring cells share most of their answer list — on the Baskonia/Treviso/Unicaja × Khimki grid three cells hold only {Garbajosa, Markovic} between them, so the top two rows consumed both and the third got nothing. It now MATCHES cells to players (bipartite matching, the same algorithm the test used to prove an assignment exists). The data was never at fault: a new test verifies all 80 grids admit nine distinct players. Previously, v73 — Path Between and Club Reveal can concede their daily too, matching The Grid: the button is offered, arms once before it commits (no second pair/club today), reveals the answer — a shortest route, or the club — and records the loss. A lost daily still counts as PLAYED, so conceding costs that game its own record but not the cross-game hub streak. Complete the Five and Career Order still refuse; they are now the only two that do. Previously, v72 — The Grid's daily can be conceded. The button already existed but was hidden on the daily (as it still is in Path Between and Club Reveal); now it's offered, reveals one valid answer per empty cell, and records the loss exactly as running out of guesses does. It arms first on the daily — there's no second grid today — but stays a single tap in practice, where nothing is irreversible. Worth knowing: a lost daily still counts as PLAYED, so conceding costs The Grid's own record but not the cross-game hub streak. Previously, v71 — Modes are addressable too: ?game=thegrid&mode=practice, and the address is rewritten whenever you switch tabs, so a copied link always matches the screen. Each of the ten games with modes gained a public goMode, and reports every mode change (tab, keyboard, or restored) back to app.js. Deliberately NOT in the sitemap, and mode URLs do NOT self-canonicalise: all ~32 serve identical HTML, so treating them as separate pages would be 32 duplicates competing with each other — a mode is a state of a game's page, not a page. Titles do name the mode ("The Grid · Practice"). Previously, v70 — Every game is now addressable. ?game= used to be stripped from the URL on load, so eleven games shared one address: nothing to copy, nothing for a crawler to tell apart. Now the URL is kept and written on every hop, each view sets its own <title>, and the canonical is rewritten to point each game URL at ITSELF (one hardcoded canonical would have told Google to discard all eleven). Adds robots.txt + sitemap.xml, and makes og:image/twitter:image absolute plus og:url — deferred until the domain was live because a relative og:image is dropped by strict scrapers. Deliberate trade: a refresh now REOPENS the game you were on rather than returning to the lobby, because that's what the address says. Previously, v69 — Four fixes. (1) Path Between and Club Reveal lose their autocomplete — in a game about naming the link, a list of candidate players (or of the twenty-odd clubs) was close to printing the answer. Typed text now resolves on its own: exact, or a partial only one name matches; anything else is reported and costs NO guess, since only a resolved name reaches the board. (2) Leaving a view now closes its dialogs — the how-to auto-opens on a game's first visit, and going Back without dismissing it left the modal floating over the hub. (3) Higher or Lower daily passes at 7/10 (was 8). (4) Odd One Out daily passes at 3/5 (was 4). Previously, v68 — Feedback opens a small modal instead of navigating straight to a mailto:. The bare mailto was a silent dead end — it only resolves if the OS has a default mail app registered, and a machine whose mail lives in a browser tab has none, so clicking did nothing at all with no way to tell. The modal always shows the address, with Copy address (works regardless of what's installed) alongside Open mail app (still prefills version + screen). Previously, v67 — Feedback gets a corner icon on the hub, because the colophon link alone was unfindable: the home view is built to FILL the viewport, so its footer sat at or below the fold — on a phone you had to scroll a screen that is designed not to scroll. The icon takes the free LEFT slot beside the theme toggle rather than becoming a third right-hand icon, which at 375px reaches the centred masthead. Previously, v66 — "Send feedback" in the colophon — a prefilled mailto whose diagnostic footer reports the version read from Cache Storage, i.e. the build the reporter's phone is ACTUALLY running, which for an offline-first PWA is often not the newest. The address is assembled at runtime so it never sits in the page source for scrapers; a test enforces that. Previously, v65 — Renamed the site to Euroball, ahead of euroballgames.com: masthead, <title>, apple-mobile-web-app-title, and the PWA manifest — which still installed to the home screen as "EuroGuess" and described a single Wordle-style game long after the site became an eleven-game hub, so every shared link undersold it. Mystery Player's share line now names the GAME, like the other ten do, rather than the site. og-image.png regenerated: "EUROBALL" sets on one line, and the card lists games instead of one game's modes. Also carries the v64 typographic pass if you skipped it: */
/* v64, if you skipped it — Typographic pass over the whole sheet: one type scale (--t-*) with tracking in em, one red and one green instead of five near-shades, 2px corners everywhere, and night mode re-inked from those tokens rather than forty hand-patched overrides. Fixes the tab underline (a 999px button radius was bending it into a swash), the emoji in the search field and colophon, the hub pack squashing tiles below a legible size on small phones, Connections' square tiles, the mid-word masthead splits (Rec|ords, Connect|ions), and grid heads breaking to "PANATHIN / AIKOS". Also fills in career timelines for 52 players who had none (49 rostered legends + 3 actives), which re-deals the generated Grid, Path Between, Connections and Odd One Out sets. */
var ASSETS = [
  "./", "index.html", "players.js", "legends.js", "careers.js", "lineups.js", "puzzles.js", "oddones.js", "grids.js", "paths.js", "clubs.js",
  "game.js", "playerid.js", "completefive.js", "connections.js", "careerorder.js", "thegrid.js", "clubreveal.js", "pathbetween.js", "oddoneout.js", "higherlower.js", "rostermaster.js", "records.js", "archive.js", "app.js",
  "manifest.webmanifest", "icon-192.png", "icon-512.png"
];

/* The generated per-game pages (build_pages.js). Each is a full copy of the app
 * with its own head and its own standing copy, so caching them is what lets an
 * offline visitor reload /the-grid/ and land back on The Grid rather than on the
 * hub — the exact-match branch below finds one of these before the fallback ever
 * runs.
 *
 * Kept SEPARATE from ASSETS, and added one at a time, because addAll is
 * all-or-nothing: a single 404 rejects the whole promise, install fails, and the
 * service worker never activates — which would take the entire offline app down
 * over one missing page. The app shell has to be that strict. These do not:
 * a page that fails to cache simply isn't available offline. */
var PAGES = [
  "mystery-player/", "player-id/", "complete-the-five/", "connections/", "career-order/",
  "the-grid/", "common-club/", "path-between/", "odd-one-out/", "higher-or-lower/", "roster-master/"
];

self.addEventListener("install", function (e) {
  e.waitUntil(
    caches.open(CACHE).then(function (c) {
      return c.addAll(ASSETS).then(function () {
        return Promise.all(PAGES.map(function (p) { return c.add(p).catch(function () {}); }));
      });
    }).then(function () { return self.skipWaiting(); })
  );
});

self.addEventListener("activate", function (e) {
  e.waitUntil(
    caches.keys().then(function (keys) {
      return Promise.all(keys.filter(function (k) { return k !== CACHE; }).map(function (k) { return caches.delete(k); }));
    }).then(function () { return self.clients.claim(); })
  );
});

function putCache(req, resp) { var copy = resp.clone(); caches.open(CACHE).then(function (c) { try { c.put(req, copy); } catch (err) {} }); return resp; }

self.addEventListener("fetch", function (e) {
  if (e.request.method !== "GET") return;
  // Cross-origin requests are none of this worker's business, and until the
  // analytics beacon there were none to prove it: the only other outside call is
  // the feedback relay, which is a POST and returns on the line above. So every
  // branch below quietly assumed same-origin, and none of them hold for a
  // third-party GET. The beacon is not a navigation and does not end in
  // .html/.js/.css, so it would fall through to the CACHE-FIRST branch, be
  // stored under the app's cache key, and — when the request failed, which for
  // an offline-first PWA is routine — be answered with index.html. A page of
  // HTML, handed to a <script> tag, and cached until the next version bump.
  var url = new URL(e.request.url);
  if (url.origin !== self.location.origin) return;
  var path = url.pathname;
  // App shell (HTML/JS/CSS + navigations) → NETWORK-FIRST so a plain reload always
  // shows the latest code when online; fall back to cache only when offline.
  var isShell = e.request.mode === "navigate" || path === "/" || /\.(?:html|js|css)$/.test(path);
  if (isShell) {
    e.respondWith(
      fetch(e.request).then(function (resp) { return putCache(e.request, resp); })
        .catch(function () { return caches.match(e.request).then(function (hit) { return hit || caches.match("index.html"); }); })
    );
    return;
  }
  // Static assets (images, manifest, icons) → CACHE-FIRST (they rarely change).
  e.respondWith(
    caches.match(e.request).then(function (hit) {
      return hit || fetch(e.request).then(function (resp) { return putCache(e.request, resp); })
        .catch(function () { return caches.match("index.html"); });
    })
  );
});
